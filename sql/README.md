# SQL Dialect

All SQL files in this folder are **MySQL dialect** for `v0`.

Current policy:

- Keep SQL raw and explicit
- Keep engine-specific repository modules in application code
- Do not use ORM

Future:

- Add PostgreSQL SQL files and repository implementation
- Select engine via `db_engine` config switch
