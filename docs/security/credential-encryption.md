# Credential Encryption

Exchange credentials must be encrypted at rest.

Requirements:

- Master key from environment variable
- No plaintext secrets in logs
- Rotation procedure documented before production rollout
